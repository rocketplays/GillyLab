{
  "name": "gillylab-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/gen-odds-page.cjs && node scripts/gen-deep-dive.cjs && bash scripts/build-site.sh",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "node scripts/test-espn-bouts.cjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
