SHIPPING THE NEW LANDING PAGE — what's done, what's left, and what will bite you
================================================================================
Written 2026-07-18 at the end of the session that built it. Read this before touching
worker/pages.js. Everything below was measured, not assumed; where a number appears, it
was checked on a real device by the owner.

  !! UPDATED 2026-07-18 (second session). READ THIS BLOCK FIRST — parts of the
  !! document below are now WRONG, and one of them was wrong when it was written.


CORRECTIONS TO THIS DOCUMENT
----------------------------
1. "It is live at /landingpagetest ... CI regenerates it every run" — WAS FALSE AS
   WRITTEN. The commit that added this file (951963ab) also flipped line ~1161 of
   gen-showcase-proto.cjs to `page(false, false)` — step 1 of THE ONE-LINE ALTERNATIVE
   at the bottom — without updating the two assertions below it, which still asserted the
   preview was noindex and carried the sliders. So the generator threw on EVERY run from
   20:25 onward. The workflow ran it as `|| true`, swallowed the throw, went green, and
   worker/landing-test.js stayed frozen at the 20:17 build. The preview was not tracking
   the card; it was a stale artifact. That is this document's own trap #5, live, in the
   file that documents it. FIXED — and then deliberately re-applied: /landingpagetest
   is gone and page(false, false) IS the shipped landing page. See SHIPPED, below.

2. The 46-check suite CANNOT RUN. scripts/test-showcase-proto.cjs requires jsdom, which
   is not in package.json and not in node_modules. CI's `npm install` will not fetch an
   undeclared dep. If it passed for you, jsdom is global on the Mac and nowhere else.
   "VERIFY BEFORE YOU PUSH" lists it as a gate. It is not one.

3. "CTAs: Start free / Go Premium / Log in" is listed under THE DESIGN, SETTLED — "picked
   by the owner looking at a real phone". It was not. The generator's own comment (~line
   943) says Go Premium replaced Compare plans because #plans did not exist in the
   PROTOTYPE'S SLICE and the button did nothing. That is a mechanical fix to a dead link,
   not a judgement. It happens to be what the owner confirmed on 2026-07-18 — but do not
   cite this doc as evidence a phone was involved.

4. FIXED — see TWO REAL BUGS, below. The stray-${} guard only covered slideJS (generator
   line ~161), never the CSS. As a result the literal string
       ${matchupFree && matchupFree.css ? matchupFree.css : ''}
   was sitting in the stylesheet at offset ~63043, carried in as raw text by the
   ${componentCSS} slice (it is pages.js line 558, sliced verbatim). Trap #2: the
   parser drops it and probably the rule after it. NOTE: matchupFree.css itself IS present
   and correct — it resolves separately at generator line ~450. I first claimed the hub
   slide was missing 14KB of CSS. That was wrong; I had mangled my own search string in a
   shell quote. The bug is the stray literal, not a missing payload. Measure, then assert.
   Stripped in the slice now, with a guard that throws if the pattern moves.


SHIPPED 2026-07-18. THE GRID IS LIVE AT "/".
--------------------------------------------
Taken via THE ONE-LINE ALTERNATIVE at the bottom of this document, not the full merge.
The owner's words: "all we need to do is remove the current landing page, take
/landingpagetest and put it as the new landing page." That is exactly what this is — and
it ships the EXACT BYTES that were approved on a phone, which no reconstruction can claim.

  worker/landing-test.js  ->  worker/landing-grid.js     (landingTestPage -> landingGridPage)
  worker/index.js         ->  `if (!s) return html(landingGridPage());` on "/"
                              /landingpagetest route + import deleted
  generator               ->  emits page(false, false): indexable, no sliders, no banner
  workflow                ->  step renamed, $FILES updated (3 refs), still regenerates
                              every run so the card/rankings/roster stay current

  .plans is GONE from the live page — it was never in the prototype. It still exists in
  landingPage() (662-731), which is no longer served. /subscribe keeps its own feature
  list, so the detail is not lost from the site.
  Hero CTAs: Start free / Go Premium / Log in.

WHAT IS STILL DEAD WEIGHT (the known cost of this route, and it is fine)
  landingPage() in pages.js — 130KB rendered, no longer served, but scripts/gen-carousel.cjs
  still slices it to build /subscribe's carousel. DO NOT DELETE IT until /subscribe has its
  own source. The freeze recipe below is the cheap way to finish that whenever you want to.
  Nothing is broken while it sits there; the Worker just carries a page nobody sees.


STEP 2 IS SOLVED — DO NOT REWIRE gen-carousel, RETIRE IT
--------------------------------------------------------
"The one that will hurt" has a cheap answer, and it was measured this session:

  - /subscribe (pages.js 1284) is a 620px column inside shell(). The grid is a full-page
    design with three fixed background layers. Pointing /subscribe at the grid is a bad
    structural fit. Take the OTHER branch: give it its own explicit source.
  - `node scripts/gen-carousel.cjs` against the CURRENT pages.js produces a
    carousel-data.js BYTE-IDENTICAL to the committed one. Verified. So the committed file
    is already exactly what /subscribe needs.
  - carousel-data.js imports landingData and matchupFree ITSELF. Freezing it does NOT
    freeze its data — the card, the names and the payloads still resolve at render. It
    only stops tracking landingPage()'s markup, which is the markup being deleted.

  So: freeze worker/carousel-data.js as a hand-owned source file (rewrite its
  AUTO-GENERATED header), delete scripts/gen-carousel.cjs, drop its workflow step, and
  drop worker/carousel-data.js from $FILES. The END CAROUSEL SCRIPT sentinel in pages.js
  (line 1207) then has no reader and goes with the carousel driver.
  Verified already: carousel-data.js parses as a module and contains no foreign exports.


THE SPLICE HAZARD THAT WILL BITE THE NEXT SESSION
-------------------------------------------------
The authored grid CSS in the generator runs from `.fx-h .a{` (line 278) to the end of the
css template (770). DO NOT PASTE THAT RANGE INTO pages.js. It is 56KB because it contains
two interpolations that are already present in pages.js:

    line 450   ${matchupFree.css || ''}      <- pages.js resolves this itself at line 558
    line 769   ${componentCSS}               <- pages.js already HAS this at 505-576

Pasting the range duplicates ~14KB of matchup CSS and the whole component block. The
authored region you actually want is 278-449 plus 451-768, with BOTH interpolations left
behind for pages.js to resolve.

And the block carries 4 backticks — inside a CSS comment at block-line 243,
"Only ever set by `grade`, never by `lead`". Harmless in a .cjs file. Trap #1 the instant
it lands in an 84KB template literal. Escape them or change the quotes.

Other deltas between the oracle (/landingpagetest render, 190,631 chars) and what the
merged landingPage() must emit:
  - NO .fx-note banner. It is dev text naming worker/pages.js and it is NOT gated on
    `debug` — the generator's "production" build emits it. It must not ship.
  - photo/logo paths go back to RELATIVE. The generator rewrites 2 photo refs and 1 logo
    ref to https://gillylab.com/ for file:// only; the Worker serves them relative.
  - NO window.SHOWCASE_PROTO. It exists only for test-showcase-proto.cjs, which is being
    deleted.
  - body keeps `animation:lpin`. The generator strips it (its keyframe isn't sliced); the
    real page has the keyframe at line 402 and the lp-out click handler at 1220-1228.
    Those are the page-transition system, orthogonal to carousel->grid. KEEP them — 399-404
    sit outside every deletion region. Dropping them would be removing a feature nobody
    asked to remove. (See the last section of this document.)

CSS deletion regions in landingPage(), measured:
    429 `.hero{`      -> 475 `.hero-cta{`            hero copy. STOP AT 475.
    480 `.showcase{`  -> 495 `.foot{text-align`      carousel chrome.
    577 `/* Free vs Premium plans */` -> 613 `</style>`   plans.
  .hero-cta and .big (475-479) MUST SURVIVE — the footer CTA reuses them. The generator
  hit this too and re-includes them in footCSS (line 251-253). `.trust` (497) becomes dead
  once the hero goes; harmless, left alone.

Body restructure: pages.js wraps EVERYTHING in one `<div class="lp">` (614 -> 751). The
design needs it SPLIT — `.lp` around the nav, `.fx-wrap` (max-width 1140) around
hero+#tiers, `.lp` again around the two footers. Script: keep `var LD=` -> 1184
`].filter(...)`, delete the driver 1186-1206 and the sentinel 1207-1218, keep 1220-1228.


WHAT THIS SESSION CHANGED
-------------------------
  scripts/gen-showcase-proto.cjs   emits the PRODUCTION page (indexable, no sliders, no
                                   banner); assertions inverted to describe "/" rather
                                   than a preview; two real bugs fixed (see below).
  worker/landing-grid.js           NEW (was landing-test.js) — the page served at "/".
  worker/index.js                  "/" serves landingGridPage(); test route + import gone.
  .github/workflows/update-odds.yml   $FILES + step renamed; and both `|| true` swallows on
                                   the landing generator replaced — continue-on-error on
                                   the step, a ::warning:: annotation in the reconcile
                                   path. The odds sweep still cannot be blocked by the
                                   landing page, but a throw is now VISIBLE.
                                   THAT SWALLOW IS THE DEFECT THAT HID EVERYTHING ABOVE.

  worker/pages.js         NEVER EDITED — verified byte-identical to session start.
  worker/carousel-data.js NEVER EDITED — verified byte-identical. /subscribe provably safe.


TWO REAL BUGS FIXED ON THE WAY OUT
----------------------------------
Both were invisible while this was a prototype nobody indexed. Both would have shipped.

  * THE PROTOTYPE BANNER WAS NOT GATED ON `debug`. <div class="fx-note"> — "Prototype. The
    previews are sliced live out of worker/pages.js" — was emitted by the "production"
    build too, and would have gone to visitors naming a source file at them. Now gated,
    with an assertion that "/" cannot carry it. (First version of that assertion tested
    /fx-note/, which also matches the harmless .fx-note{} CSS rule, so it threw on a page
    that had already dropped the banner. Assert on the markup, not the class name.)

  * THE STRAY ${} IN THE STYLESHEET. The guard at line ~161 only ever covered slideJS, so
    ${componentCSS} carried pages.js line 558 across verbatim:
        ${matchupFree && matchupFree.css ? matchupFree.css : ''}
    That sat in the CSS where a selector should be — trap #2, parser drops it and the rule
    after it, silently. Now stripped in the slice (matchupFree.css is already interpolated
    separately further up), with a guard that throws if the pattern ever moves.

VERIFIED, BY RENDERING RATHER THAN GREPPING
  check-worker-parses 10/10 · generator runs clean · workflow YAML parses
  landingGridPage()  190,190 chars · indexable · no sliders · no banner · no stray ${}
                     · #mh-overlay present exactly once · grid #tiers present
  subscribePage()    119,110 chars · carousel intact · no unresolved ${}
  landingPage()      130,237 chars · unserved · still feeds gen-carousel

NOT DONE, AND NOT NEEDED FOR THIS SHIP: the full merge into landingPage() (steps 1-4).
The splice map above is kept for whenever /subscribe gets its own source and landingPage()
can finally go. There is no rush; nothing is broken while it sits there.

  !! END OF THE 2026-07-18 UPDATE. Everything below is the original document, except
  !! where the corrections above contradict it.


WHAT EXISTS RIGHT NOW
---------------------
The design is FINISHED and APPROVED. It is live at /landingpagetest (noindex, logged-out
reachable) and it is not a mockup — every feature preview is the real slide payload,
sliced out of worker/pages.js at build time, so it shows the product and cannot drift
from it. CI regenerates it every run, so the main-event deep dive / rankings / roster are
always the current card.

  scripts/gen-showcase-proto.cjs   the generator. Slices the slide payloads, the nav, the
                                   footers and the CSS out of landingPage(), then builds
                                   the grid around them. Emits TWO files:
  prototypes/landing-showcase.html   local file, WITH debug sliders
  worker/landing-test.js             the Worker module, no sliders, exports landingTestPage()
  scripts/test-showcase-proto.cjs  46 checks over the generated page
  worker/landing-legacy-backup.js  frozen byte-identical copy of the CAROUSEL page, with
                                   restore instructions in its header. NOT imported, NOT
                                   served, costs the Worker nothing.

The route is worker/index.js: `if (path === "/landingpagetest") return html(landingTestPage());`
sitting with /about and /terms, i.e. outside the session gate.


THE DESIGN, SETTLED
-------------------
Do not re-derive these. Each one was picked by the owner looking at a real phone after I
picked a wrong one from arithmetic.

  frame --cy        86px      (FRAME_CY in the generator; feeds CSS *and* slider)
  aurora            90%       (AURORA = 0.90; bakes .081/.063/.072 from .09/.07/.08)
  hero              eyebrow "Every stat · Every matchup · Every edge"
                    h1 "The Ultimate UFC Analytics Database"
                    hook "Know the fight before it starts."
                    CTAs: Start free / Go Premium / Log in
  groups, in order  Free                     · play, predict and dig in — free forever
                    Before the fight         · where the research begins        [PREMIUM]
                    Betting tools            · become a smarter, more efficient bettor [PREMIUM]
                    Complete fighter history · every fighter, every bout        [PREMIUM]
  tier badge        on the group HEADING, premium only. No badge = free. No per-card pills
                    (15 pills repeating their own heading), no Free/Premium jump chips
                    (Free is one row; the chip saved a quarter of a swipe).
  desktop           2-up grid from 720px, previews 290px, hub card spans the full row
  mobile            each group is a horizontal snap ROW. 86vw cards -> ~14% of the next
                    card peeks. THE PEEK IS THE ENTIRE AFFORDANCE. Do not "tidy" it to
                    100vw; a row that ends flush at the screen edge looks like it ends and
                    nobody swipes it. ~2.9 screens vs 5.7 stacked.


WHAT'S LEFT — FOUR THINGS, AND THEY MUST LAND TOGETHER
------------------------------------------------------
1. Move the grid into landingPage() in worker/pages.js.
   Replace the hero (badge/h1/sub/hero-cta/trust) and the whole <section class="showcase">
   + its carousel driver. Add the ~5KB of .fx-* CSS, the three background layers
   (#bgfx aurora / #bgframe octagon+rails / #bgedge the edge wash), the theme-color meta
   and viewport-fit=cover. All of it is in gen-showcase-proto.cjs already — it is a move,
   not a rewrite.

2. REWIRE gen-carousel.cjs IN THE SAME COMMIT. This is the one that will hurt.
   It slices landingPage()'s showcase markup + CSS + slide script for /subscribe. The
   moment / stops having a carousel, that slice either throws or silently emits garbage.
   It already did exactly this today when the FAQ moved: carousel-data.js came out
   containing `export const signupPage` and /subscribe would not have built. Either point
   /subscribe at the grid too, or give it its own explicit source.

3. Delete the test page: worker/landing-test.js, its import + route in worker/index.js,
   the "Regenerate the landing page preview" step in .github/workflows/update-odds.yml
   (both the normal path AND the reconcile path near the bottom), and worker/landing-test.js
   from $FILES (also two places).

4. Decide what gen-showcase-proto.cjs becomes. Once the design lives in pages.js it either
   goes away or turns into a preview of the real thing. prototypes/landing-showcase.html
   goes with it.

Do NOT delete landingPage() until step 2 is done — gen-carousel still eats it.


THE TRAPS. ALL OF THESE HAPPENED TODAY.
---------------------------------------
* BACKTICKS IN COMMENTS. landingPage() is one 84KB template literal. A backtick in a
  comment CLOSES it and the rest of the file parses as code. This broke pages.js SEVEN
  times in one session, four of those in comments warning about it. `node --check` does
  NOT catch it — it assumes CommonJS and exits 0. The check that does:
      node scripts/check-worker-parses.cjs        <- runs in CI before the deploy
  It names the offending line and quotes it. Use it after every edit to pages.js.

* CSS AND HTML FAIL SILENTLY TOO. A stray */ makes the CSS parser drop every rule after
  it (this is why the debug panels once rendered as text at the top of the page). An
  unbalanced <!-- publishes your prose. Neither throws. gen-showcase-proto.cjs now refuses
  to emit if CSS comments are unbalanced or prose sits where a selector should be.

* MARKERS THAT ARE ALSO CODE MOVE WITH THE CODE. gen-carousel used the FAQ's first comment
  as its end marker; when the FAQ moved to /faq the marker went with it and the slice ate
  three other pages. pages.js now carries an inert sentinel:
      /* ===== END CAROUSEL SCRIPT ===== */
  Do not delete it. Do not let it drift from the end of the script.

* indexOf FINDS THE WRONG PAGE. pages.js holds every page; landingPage starts ~26k chars
  in. A plain indexOf finds some OTHER page's copy of shared markup — `.brand{` appears 5
  times, `<footer class="site-footer">` twice with DIFFERENT link lists. gen-showcase-proto
  anchors every slice from the landingPage offset. Do the same.

* A GENERATOR THAT THROWS LEAVES A STALE ARTIFACT, AND THE TESTS PASS ON IT. Twice today
  the generator errored and the suite reported 46/46 against the previous build. If you
  change the generator, read its output line, not just the test summary.

* jsdom LIES ABOUT CSS. It silently drops what it cannot parse — min(), linear-gradient,
  calc(). It told me #mh-box had no width when the file says width:min(1040px,94vw), and
  that .fx-bl had no background. "Could not parse CSS stylesheet" on stderr means it is
  discarding declarations. For CSS facts, grep the raw text or use a real browser.

* THE HUB SLIDE IS SPECIAL. The matchup payload is authored for the modal's real 1040px and
  CANNOT reflow (four fixed grid tracks). It needs `.mh-slide{--mh-rail:3.4rem}` or the
  tiles stack in one column — --mh-rail lives on #mh-box, which the slide can't render
  because that id would exist three times. It also gates itself on
  matchupFree.slug === landingData.card.slug so a stale file can't advertise a finished
  fight. Keep both.


VERIFY BEFORE YOU PUSH
----------------------
    node scripts/check-worker-parses.cjs     # must be 10 of 10. Catches the backtick bug.
    node scripts/test-showcase-proto.cjs     # 46 checks (delete with the prototype)
    node scripts/gen-carousel.cjs            # must not emit another page's exports
    node --input-type=module --check < worker/carousel-data.js

And render the thing rather than grepping for it — three times today a check passed or
failed on its own grammar rather than on reality:
    node -e 'import("./worker/pages.js").then(m => console.log(m.landingPage().length))'


IF IT GOES WRONG
----------------
worker/landing-legacy-backup.js is the carousel page, byte-identical, with restore steps in
its header. Copy the export over landingPage(), re-run gen-carousel.cjs, push. Git has it
too but a file with BACKUP in the name is a better answer at 2am.


THE ONE-LINE ALTERNATIVE, IF YOU WANT IT LIVE TODAY
---------------------------------------------------
Point / at the generated page instead of merging:
  - gen-showcase-proto.cjs: emit page(false, false) — no debug chrome, no noindex
  - worker/index.js: `if (!s) return html(landingGridPage());` in the "/" route
  - drop the /landingpagetest route
This ships the approved page with zero risk to /subscribe, because landingPage() stays
put and keeps feeding gen-carousel. The cost: landingPage() is dead weight in the bundle
(~84KB) and /subscribe still shows a carousel of the same features the front page now
shows as a grid. It is a real ship, not a hack — but steps 1-4 above are still the finish.


ONE HONEST NOTE ON PROCESS
--------------------------
Nearly every visual call in this design was made by the owner looking at a phone, after I
proposed a wrong one from arithmetic: the octagon size, the aurora twice in opposite
directions, the stroke width, the frame height, the safe-area bands. The measurements I
CAN make are reliable (page heights, alphas, geometry, what a slice contains). The
judgements are not mine. When his eyes and my arithmetic disagree, the arithmetic is
describing a different problem than the one on screen — I broke an approved design once
today by "fixing" a defect he had never reported. Don't do that.
